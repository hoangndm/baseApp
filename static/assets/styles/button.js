import { StyleSheet } from 'react-native';

export default btn = StyleSheet.create({
    normal: {
        padding: 10,
        borderRadius: 15,
        width: '100%',
        justifyContent: 'center',
        alignItems: 'center'
    },
    icon: {
        width: 25,
        height: 25,
        marginRight: 5
    },
    small: {
        width: 20,
        height: 20,
        justifyContent: 'center',
        alignItems: 'center',
        borderRadius: 10
    }
});